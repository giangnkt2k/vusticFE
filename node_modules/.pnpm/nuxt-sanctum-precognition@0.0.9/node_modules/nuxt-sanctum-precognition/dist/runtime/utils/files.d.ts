/**
 * Check if the given value is a File or a Blob.
 * @param value
 */
export declare const isFile: (value: unknown) => boolean;
/**
 * Check if the given data contains any files.
 * @param data
 */
export declare const hasFiles: (data: unknown) => boolean;
/**
 * Removes all files from the given data object recursively.
 * @param data
 */
export declare const clearFiles: (data: object) => object;
