import type { Payload, PrecognitionForm } from '../types.js';
export interface FormError<P extends string = string> {
    name?: P;
    message: string;
}
export declare const useNuxtFormValidator: <T extends Payload>(form: PrecognitionForm<T>) => () => Promise<FormError<string>[]>;
